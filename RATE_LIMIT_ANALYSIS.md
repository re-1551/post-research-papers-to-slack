# Gemini 2.0 Flash Lite レート制限分析と対策

## 📋 Gemini 2.0 Flash Lite のレート制限

| 制限項目 | 上限値 | 説明 |
|---------|--------|------|
| **RPM** | 30 | Requests Per Minute (1分あたりのリクエスト数) |
| **TPM** | 1,000,000 | Tokens Per Minute (1分あたりのトークン数) |
| **RPD** | 200 | Requests Per Day (1日あたりのリクエスト数) |

## 🔍 現在の実装でのAPI使用量分析

### 1. Daily Digest ジョブ（1日1回実行）

#### 使用パターン
- **タイトル翻訳**: 各論文につき1リクエスト
- **想定論文数**: 最大100件（実際は10-50件程度）

#### API呼び出し回数
```
論文数 × 1回（タイトル翻訳） = 最大100リクエスト/日
```

### 2. ボタンクリック時の概要生成

#### 使用パターン
- **概要生成**: ユーザーがボタンを押した時のみ
- **頻度**: 不定期（ユーザー依存）

#### 想定API呼び出し回数
```
推定: 10-30リクエスト/日（ユーザー数・関心度に依存）
```

### 3. 合計API使用量（推定）

```
Daily Digest:    100リクエスト/日（最大）
概要生成:         30リクエスト/日（推定）
--------------------------------
合計:            130リクエスト/日
```

**✅ RPD制限（200）には余裕あり**

## ⚠️ 問題点の発見

### 🔴 **RPM制限違反の可能性**

#### 修正前の実装
```typescript
for (let i = 0; i < papers.length; i++) {
  const titleJa = await translateTitle(paper.title); // Gemini API呼び出し
  await channel.send(...);
  
  // 1秒待機（Discord API対策のみ）
  await new Promise(resolve => setTimeout(resolve, 1000));
}
```

#### 問題点
- **待機時間**: 1秒 = 60リクエスト/分の可能性
- **RPM上限**: 30リクエスト/分
- **超過率**: **2倍の超過** ❌

#### 計算例
```
100論文を処理する場合:
- 最初の1分間: 60リクエスト
- RPM上限: 30リクエスト
- 超過: 30リクエスト（100%超過）
```

### 🔴 **TPM制限の可能性**

#### トークン数の推定
- **タイトル翻訳**: 約100-200トークン/リクエスト（入力+出力）
- **概要生成**: 約1,000-2,000トークン/リクエスト

#### 計算例（最悪ケース）
```
100論文 × 200トークン（タイトル翻訳）= 20,000トークン
概要生成 30回 × 2,000トークン = 60,000トークン
--------------------------------
合計: 80,000トークン/日
```

**✅ TPM制限（1,000,000）には余裕あり**

ただし、短時間に集中した場合:
```
100論文を100秒で処理（1秒間隔）:
100リクエスト × 200トークン = 20,000トークン/100秒
→ 12,000トークン/分（1.2%使用）
```

**✅ TPMも問題なし**

## ✅ 実装した対策

### 1. レート制限管理の実装

```typescript
// Gemini 2.0 Flash Lite のレート制限設定
const GEMINI_RPM_LIMIT = 24; // 安全のため80%に設定（30の80%）
const RATE_LIMIT_WINDOW_MS = 60 * 1000; // 1分

function calculateRateLimitDelay(requestCount: number): number {
  // 均等に分散させるための待機時間
  return RATE_LIMIT_WINDOW_MS / GEMINI_RPM_LIMIT;
  // = 60000ms / 24 = 2500ms (2.5秒)
}
```

### 2. リクエストカウンターの実装

```typescript
let requestCount = 0;
let windowStartTime = Date.now();

for (let i = 0; i < papers.length; i++) {
  // 1分ごとにカウンターをリセット
  const currentTime = Date.now();
  if (currentTime - windowStartTime >= RATE_LIMIT_WINDOW_MS) {
    requestCount = 0;
    windowStartTime = currentTime;
  }
  
  // レート制限による待機（2.5秒）
  if (requestCount > 0) {
    const delay = calculateRateLimitDelay(requestCount);
    await new Promise(resolve => setTimeout(resolve, delay));
  }
  
  // API呼び出し
  const titleJa = await translateTitle(paper.title);
  requestCount++;
}
```

### 3. モデル名の更新

```typescript
// 修正前
const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' });

// 修正後
const model = genAI.getGenerativeModel({ model: 'gemini-2.0-flash-lite' });
```

## 📊 対策後の性能分析

### 処理時間の計算

#### 100論文を処理する場合
```
リクエスト間隔: 2.5秒
100論文 × 2.5秒 = 250秒 = 約4.2分

実際には:
- 1分目: 24リクエスト（60秒）
- 2分目: 24リクエスト（60秒）
- 3分目: 24リクエスト（60秒）
- 4分目: 24リクエスト（60秒）
- 5分目: 4リクエスト（10秒）
合計: 約4分10秒
```

#### RPM遵守の確認
```
1分あたり: 24リクエスト
RPM上限: 30リクエスト
余裕: 6リクエスト（20%）✅
```

### 典型的なケース（30論文）
```
30論文 × 2.5秒 = 75秒 = 1.25分

- 1分目: 24リクエスト
- 2分目: 6リクエスト
合計: 約1分15秒
```

## 🎯 結論

### ✅ レート制限の遵守状況

| 制限項目 | 上限 | 使用量（最大） | 余裕 | 状態 |
|---------|------|---------------|------|------|
| **RPM** | 30 | 24 | 20% | ✅ 安全 |
| **TPM** | 1,000,000 | ~24,000 | 97.6% | ✅ 非常に安全 |
| **RPD** | 200 | ~130 | 35% | ✅ 安全 |

### 📈 性能への影響

- **処理時間**: 約1.25分（30論文）〜 4.2分（100論文）
- **ユーザー体験**: Daily Digestは定時実行のため、処理時間は問題なし
- **信頼性**: レート制限エラーのリスクを大幅に削減

### ⚡ Gemini 2.0 Flash Lite の利点

1. **高速処理**: RPM 30 により、従来の2倍の速度で処理可能
2. **コスト効率**: Liteモデルのため、より経済的
3. **十分な性能**: タイトル翻訳や概要生成には十分な品質

### 🔧 今後の改善案

1. **動的なレート制限調整**
   - エラー発生時に自動的に待機時間を延長
   - 成功率に応じて最適化

2. **バッチ処理の最適化**
   - 論文数が少ない場合は待機時間を短縮
   - 論文数が多い場合は段階的に投稿

3. **モニタリングの強化**
   - API使用量のログ記録
   - レート制限エラーの検知と通知

## 🚀 推奨事項

1. **現在の実装で問題なし**: RPM制限を安全に遵守
2. **監視の継続**: ログでAPI使用状況を定期的に確認
3. **将来的な拡張**: ユーザー数が増えた場合の対策を検討
